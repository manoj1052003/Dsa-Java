package com.basic;

import java.util.Arrays;

public class Class3 {

	public static char[] conv_chararray(String s) {
		char[] arr = new char[s.length()];
		for (int i = 0; i < s.length(); i++) {
			arr[i] = s.charAt(i);
		}
		return arr;
	}

	public static void conv_upper(String s) {
		String res = "";
		for (int i = 0; i < s.length(); i++) {
			char ch = s.charAt(i);
			if (ch >= 97 && ch <= 122) {
				// conv upper case
			} else {
				// no conv
			}
		}
	}

	public static void main(String[] args) {
		String s = "PenTAgoN";
		
		char ch = s.charAt(1);
		System.out.println(ch);

	}

}
